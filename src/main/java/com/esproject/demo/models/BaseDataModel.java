package com.esproject.demo.models;

/**
 * 3 models: BankAccountDetails, Shakespeare, Sales
 * do we need abstract class or interface?
 *
 * document id -> getId, pass index
 *
 */
public interface BaseDataModel {
    String getIndex();
}
